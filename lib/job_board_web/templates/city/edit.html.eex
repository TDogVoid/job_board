<h2>Edit City</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.city_path(@conn, :update, @city)) %>

<span><%= link "Back", to: Routes.city_path(@conn, :index), class: "btn btn-primary btn-xs" %></span>
