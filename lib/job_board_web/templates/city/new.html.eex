<h2>New City</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.city_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.city_path(@conn, :index), class: "btn btn-primary btn-xs" %></span>
